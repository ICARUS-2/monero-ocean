export default class PortAlgorithm
{
    port: string;
    algorithm: string;

    constructor(port: string = "", algorithm: string = "")
    {
        this.port = port;
        this.algorithm = algorithm;
    }
}